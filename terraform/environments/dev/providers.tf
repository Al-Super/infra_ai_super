terraform {
  required_providers {
    digitalocean = {
      source  = "digitalocean/digitalocean"
      version = "~> 2.0"
    }
  }
}

provider "digitalocean" {
  # Token can be provided via environment variable DIGITALOCEAN_TOKEN
  token = var.do_token
}
